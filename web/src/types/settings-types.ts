export interface IDepartmentSettings {
    id: string;
    name: string;
}
  
export interface IEventType {
    id: string;
    name: string;
}

export interface ITemplate {
    id: string;
    name: string;
    description?: string;
    category: string;
}